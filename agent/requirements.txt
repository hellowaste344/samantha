# ═══════════════════════════════════════════════════════════════════════════════
#  Samantha — Python requirements
# ═══════════════════════════════════════════════════════════════════════════════

# ── Core ──────────────────────────────────────────────────────────────────────
python-dotenv>=1.0.0
pydantic>=2.0.0
rich>=13.0.0

# ── LLM — Ollama HTTP client ──────────────────────────────────────────────────
httpx>=0.27.0

# ── STT — faster-whisper (CTranslate2, 4× faster than openai-whisper) ─────────
faster-whisper>=1.0.0
soundfile>=0.12.1

# ── Audio capture (Python fallback mode only — STT_MODE=voice) ────────────────
sounddevice>=0.4.6
numpy>=1.24.0

# ── TTS — edge-tts (neural, 300+ voices, zero system deps) ───────────────────
edge-tts>=6.1.9

# ── TTS audio playback ────────────────────────────────────────────────────────
pydub>=0.25.1      # primary playback backend (needs system ffmpeg)
# sounddevice above doubles as playback backend

# ── TTS — piper-tts (OFFLINE fallback, needs espeak-ng) ──────────────────────
# Uncomment to enable:
# piper-tts>=1.2.0

# ── pyttsx3 — last-resort TTS fallback ───────────────────────────────────────
pyttsx3>=2.90

# ── Screen capture pipeline ───────────────────────────────────────────────────
# Capture: MSS — low-latency cross-platform screen grab (< 5 ms/frame)
mss>=9.0.1

# Processing: OpenCV
opencv-python>=4.8.0

# UI element detection: YOLOv8 (model weights auto-downloaded on first use)
# Default model: yolov8n.pt (~6 MB, COCO classes, ~10 ms/frame on CPU)
# Swap YOLO_MODEL_PATH in .env for a fine-tuned UI-detection model.
ultralytics>=8.0.0

# OCR primary: PaddleOCR
# Installed separately by `make install` with a non-fatal fallback
# because paddlepaddle does not publish wheels for every Python version.
# If install fails, screen.py falls back to Tesseract automatically.
# To install manually: pip install paddlepaddle paddleocr

# OCR fallback: pytesseract
# Also requires the system Tesseract binary:
#   Ubuntu/Debian: sudo apt install tesseract-ocr
#   Arch Linux:    sudo pacman -S tesseract tesseract-data-eng
#   macOS:         brew install tesseract
#   Windows:       https://github.com/UB-Mannheim/tesseract/wiki
pytesseract>=0.3.10

# ── Browser automation ────────────────────────────────────────────────────────
playwright>=1.44.0
# After install: python -m playwright install chromium

# ── OS automation ─────────────────────────────────────────────────────────────
pyautogui>=0.9.54
pyperclip>=1.8.2

# ── Knowledge tools ───────────────────────────────────────────────────────────
wikipedia>=1.4.0

# ── Local API bridge (dashboard WebSocket + REST) ─────────────────────────────
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
websockets>=12.0

# ── Email (read/summarize) — future Gmail API integration ────────────────────
# Uncomment when adding Gmail read support:
# google-auth>=2.29.0
# google-auth-oauthlib>=1.2.0
# google-api-python-client>=2.127.0
